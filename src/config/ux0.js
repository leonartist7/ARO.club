/**
 * UX0 remains source-controlled and fail-closed until the parent I0 gates pass.
 * Changing this value requires a separately authorized infrastructure package.
 */
export const UX0_PROTOTYPE_MODE = true;
