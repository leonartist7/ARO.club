# Ready-to-paste receiving-model prompt

You are continuing ARO's frontend implementation as a senior product engineer and design implementer. The founder wants a beautiful, clear, tactile, responsive experience across iPhone, Android and desktop. Preserve ARO's adult, warm Living Opportunity OS identity, Tonguee/Coco heritage and meaningful real-world purpose.

Repository: `leonartist7/ARO.club`.
Architecture handoff branch: `docs/aro-frontend-architect-handoff-20260923`.
Entry document: `docs/frontend-handoff-20260923/START-HERE.md`.

First fetch the handoff branch and pin its SHA. Read `AGENTS.md` and its required governing chain, then every file linked by START-HERE. Check live main, PR60, PR61 and controller branch before selecting an implementation base. The observed EF1 source was `fec6492c3b54952b1dbb048fc60457a6402ec7cd`; observed main was `0661589affcf54a1324e489b09afb213a1a4019f`. These are historical reference points, not permission to overwrite newer work.

Start with EF-Q: produce the exact source/ownership reconciliation and outstanding verification record. Existing EF1 CI was green; full route/device/accessibility/performance and independent acceptance were not complete. Do not invent a defect or claim these gaps have passed.

Then choose the first eligible adopted package in this order: EF-P participant journey, EF-C creator studio, EF-I identity/Passport, EF-S Season, EF-X expression. These proposals require package adoption under the repository process. Do not self-promote PROPOSED to SPEC-READY. Reconcile older FV2/FV3 proposals and F7 ownership. When a runtime gate is closed, finish its concrete adoption packet and other authorized preparation, reporting the exact remaining gate rather than asking a vague “shall I continue?”

For an eligible implementation package, use one branch/PR and its exact file allowlist. Baseline first. Implement the entire specified local interaction, including phone layout, EN/FR/ES, dark mode, keyboard, reduced motion, empty/invalid/image-failure states and back/direct-entry behavior. Reuse existing primitives and assets. Generate art only for a defined consuming slot using ASSET-DIRECTION; inspect and optimize it before integration. Keep essential UI semantic and outside artwork.

Never turn a preview into a real purchase, reward, commitment, AI action, location feature, public identity or persistent inventory without the governing specialist package. Preserve account boundaries, RLS, verified publishing and the P1–P5 sequence. Do not add dependencies, prices, fake urgency, artificial progress, paid randomness or Trust scoring.

Verify against every criterion in PACKAGES and ACCEPTANCE, using the exact final SHA. Fix concrete in-scope defects. Deliver the implementation PR with tested evidence, screenshots, performance comparison, remaining blockers and rollback. Do not merge, release, self-approve independent review, or start a second package to conceal incomplete evidence. Stop after the bounded package is reviewable, unless further eligible implementation work has been explicitly assigned.

Write concise progress updates and a final report that distinguishes IMPLEMENTED, VERIFIED and SHIPPED. Do not describe a whole ecosystem as complete because its preview pages render.
