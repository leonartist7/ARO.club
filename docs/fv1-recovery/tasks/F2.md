# F2 — Shell, navigation and recovery

Status: PROPOSED / SPEC-REQUIRED; not dispatched.

## Exact base revision and governing specification

- Proposal/source base: `6495e67798ad14876f6c04815f9f5e13eaf52b83`.
- Audit evidence base: `06730c74b44d1a6ee1da24c4d1d1ed721313df5c`.
- Governing proposal: [FV-1 v0.1.0](../proposed-ARO-FV-1.md), including all shared acceptance and stop rules.
- Existing governing package: `specs/ARO-UX0-OPPORTUNITY-FORMATION-PROTOTYPE.md` at the proposal base; also the authority chain listed in FV-1. UX0–UX3 boundaries all remain effective.
- Required approved implementation spec: `specs/ARO-FV-1-VISUAL-RELEASE-EVIDENCE.md` at a recorded full `APPROVED_SPEC_SHA`. Approval is absent; do not infer it from this documentation PR.
- Execution TASK_BASE_SHA: not yet available; must be the accepted F1 commit. Record that full SHA in a dispatch receipt before starting; the proposal base above is not a substitute for the predecessor.

## Dependencies

All five validated H0 reports, recorded founder/product-design approval, frozen budgets and exact approved spec revision, accepted F1 and all preceding slices. One writer at a time, one FV-1 package branch/PR. F1 freezes AppImage; F2 freezes shared recovery/navigation primitives; F3 freezes fixtures. Later consumers do not edit another owner's files.

## Permitted files, expected behavior and acceptance evidence

Files: `src/components/app/AppShell.jsx`, `src/components/app/AppPrimitives.jsx`, `src/lib/routes.jsx`; new `src/pages/AppNotFoundPage.jsx`, `src/i18n/fv1/shell.js`, `src/components/app/AppShell.test.jsx`.

> Preserve the five existing destinations. World owns opportunity/Circle routes; Insights owns Passport. Correct Create’s Close to World. Add truthful prototype notice, 44px shared targets, skip/main focus and unknown-`/app` recovery. Explain unavailable search/notification previews without implementing services. Preserve legacy routes.

Acceptance/evidence: route ownership, accessible names, exit destination, skip focus, unknown-route recovery and keyboard/back-navigation checks.

## Non-goals

No sixth tab, new navigation taxonomy, working search/notifications or legacy-route edits. No APIs, Auth, schema/RLS, persistence, money, AI, location, 3D/AR, Seasons, Beacons, dependency/provider changes, deployment or unrelated status upgrades.

## Acceptance commands and required evidence

`npm test -- --run src/components/app/AppShell.test.jsx`

Apply the relevant numbered acceptance rows in the governing proposal. Record commands, exit codes, exact tested commit, hashes and retrievable output references under an external evidence directory named F2; F7 consolidates these into the package evidence files. Preserve originals and failures. The finished package must pass lint, unit tests, build and existing required CI; independent review follows the complete diff. A screenshot is not proof of keyboard, network, state or screen-reader behavior.

## Stop conditions

Stop on missing recorded approval or exact execution/spec SHA; dirty/unowned changes; source/evidence hash mismatch; unavailable predecessor interface; any unlisted write; a failed budget or mandatory acceptance criterion; missing verification capability; or a new product decision. No sixth tab, new navigation taxonomy, working search/notifications or legacy-route edits. Route corrections to the owning slice serially. One retry for a transient tool failure; no repeated audits or coordinator loops.
