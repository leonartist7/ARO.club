# AUTO0 Next.js compatibility repair

Claim AUTO0-NEXT-20260921-001. Authority: existing ARO-AUTO0 v1.0 repository-tooling contract, founder-approved Shipaton implementation and merged N1 migration. No product/provider authority. Owner: existing N1 delivery owner acting in separate tooling lane. Base b44c82f59a339c0c240894c981401625df9456de. Branch codex/auto0-nextjs-compat-20260921; new isolated checkout/output/dependencies, new draft PR; preserve merged N1 checkout.

Proven problem: main capture35611485275/job106371438181 stops before install/build/browser because inventory calls safeFile on removed src/lib/routes.jsx. Separate source-proven adapter incompatibility: it launches Vite preview although build now emits Next .next. Do not replay failed unchanged run.

Write allowlist: tools/autonomy/core.mjs,browser.mjs,core.test.mjs; new tools/autonomy/framework.mjs and framework.test.mjs only if needed for pure shared framework detection/testing; tools/autonomy/README.md; new specs/ARO-AUTO0-NEXTJS-COMPAT.md; artifacts/ARO-AUTO0-NEXTJS/; relevant AUTO0-only entries in existing registry/current-state/changelog docs after confirming no concurrent owner. No workflow, product src,package/lockfile,provider,database,global authority or memory-policy change. If workflow edits become necessary, return precise reason before editing.

Before code, bind this scope in versioned SPEC-READY tooling package under existing AUTO0 authority. Inspect governing chain and Next accounts/build boundary. Acceptance:

1. Deterministic committed-source inventory supports current src/app page files and historical Vite routes.jsx. Framework comes from validated committed source, not arbitrary shell or memory. Route groups do not become URL segments; preserve dynamic/catchall notation and source provenance. Document scope for unsupported route conventions; fail closed for unsupported/ambiguous framework or missing/escaping/uncommitted inputs. Do not treat inventory as executable routing acceptance.
2. Browser adapter starts matching installed framework executable on fixed127.0.0.1:5199: Next start for current build, Vite preview for supported historical sources. Retain server-ownership/port-conflict checks, cleanup, exactSHA/clean-tree/root separation, same route/viewport/theme/assertion matrix, request restrictions and evidence manifest.
3. Synthetic-only proof remains real for Next, not merely stale UX0 constant. Reject enabled live-account/provider configuration; inspect build-time versus runtime variables and local-env loading. No live requests, secret output or provider writes. If built artifact cannot be safely proven synthetic within allowlist, stop with exact proposal; no guard deletion.
4. Meaningful adversarial tooling tests cover both inventories/launch selection, route groups/dynamic segments, missing/escaping/dirty/untracked source and unsupported framework; existing boundary tests remain. No dependency added. Run required lint/build and focused tooling tests. Required capture CI must collect same synthetic route matrix on exact candidate, with source unchanged before/after; do not run unrelated historical audits.
5. Independent finished-diff review checks source/evidence boundaries and final CI before acceptance. Report immutable base/head,changedFiles,commands/exits,framework/inventory evidence,artifact hash,limits and next state. No product bundle change. Metadata target stays<30s excluding clone/install/network. One bounded implementation/verification attempt; stop on unexplained failure or new authority needs, no blind reruns.

No merge/deploy/schedule/provider activation. Runtime N1/I02-08/F7/human/store gates remain independent. Restore tooling through ordinary protected PR; founder merge remains separate. No post-merge backport onto already-merged N1 branch.
