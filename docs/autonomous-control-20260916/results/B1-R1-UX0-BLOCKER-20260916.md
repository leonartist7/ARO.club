# B1 repair-cycle 1 blocker result — 2026-09-16

**Task / claim:** `B1-I02-repair-within-contract` / `B1-R1-20260916-001`  
**Candidate head:** `998b39b7fc9af32d632882442a53dad47e8710ce`  
**State:** **BLOCKED** — no source edits were made in repair cycle 1.

## Exact evidence

1. `tools/ci/browser.mjs` imports `UX0_PROTOTYPE_MODE` and returns from its prototype browser branch when the value is true.
2. `src/config/ux0.js` hard-codes `UX0_PROTOTYPE_MODE = true`.
3. The compiled app therefore fails closed: Supabase is unconfigured, login is disabled, and protected onboarding redirects. The disposable CI artifact’s four `*-prototype-boundary.png` screenshots are expected coverage for that guard, not B1 authenticated evidence.

B1’s closed allowlist does not include `src/config/ux0.js`, Vite/test-artifact configuration, or workflows. A browser-only workaround would either remain in the prototype branch or override the guard and fabricate evidence.

## Required decision

A founder-approved, separately scoped test-only UX0/I0 integration amendment is required before any further R7 implementation. The concrete proposed amendment is [I02 R7 UX0 CI amendment](../I02-R7-UX0-CI-AMENDMENT-PROPOSED-20260916.md).

No independent final review can begin while the required R7 journey is unproven. B1 has used one of its two allowed repair cycles.
